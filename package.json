{
  "name": "lido",
  "version": "0.1.0",
  "private": true,
  "description": "Lido",
  "main": "server",
  "scripts": {
    "build": "gulp build --silent",
    "deploy": "gulp deploy --silent",
    "start": "gulp server --silent"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "author": "Steve Davis <steve@celery.club>",
  "license": "MIT",
  "dependencies": {
    "gulp": "^3.9.1",
    "lorry": "0.1.0"
  }
}
